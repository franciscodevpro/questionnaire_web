export type DeviceRequestType = {
  name: string;
  pin: string;
};

export type DeviceRequestResultType = {
  id: string;
  name: string;
  pin: string;
};

export type DeviceResponseType = {
  id: string;
  name: string;
  pin: string;
};
